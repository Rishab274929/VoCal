<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Mic + speech recognizer (Tier 1 voice capture). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Camera capture for the meal photo / body-fat photo flows. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+ scoped image read (gallery picker on modern devices). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Pre-Android-13 fallback for the gallery picker; capped at API 32 so
         it doesn't request the legacy "all files" scope on modern devices. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- Foreground service for the "live activity" ongoing notification
         (VocalSessionService). dataSync subtype because we're mirroring
         the user's current data to a notification surface; required on
         API 34+ which gates FGS by sub-type. POST_NOTIFICATIONS on
         API 33+ so the notification can show at all. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Camera is optional — the user can stick to voice-only logging. -->
    <uses-feature android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature android:name="android.hardware.microphone"
        android:required="false"/>

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>

    <application
        android:label="VoCal"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Home-screen widget — iOS VoCalWidget parity. Reads today's
             calories + protein from FlutterSharedPreferences and shows a
             progress ring + macro line. -->
        <receiver
            android:name=".VocalWidgetProvider"
            android:exported="true"
            android:label="VoCal">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <!-- Our own push-refresh action sent by MainActivity
                     when Flutter mutates the macros snapshot. -->
                <action android:name="best.vocal.vocal.WIDGET_RELOAD"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vocal_widget_info"/>
        </receiver>

        <!-- Foreground "live activity" service — iOS Live Activity /
             Dynamic Island parity. Pins an ongoing notification with
             today's calorie + protein progress until Flutter calls
             stopVocalSessionService. -->
        <service
            android:name=".VocalSessionService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

